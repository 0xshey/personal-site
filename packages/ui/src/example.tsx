// Placeholder so the package typechecks; replace with real shared
// components (e.g. shadcn/ui) when two apps need the same primitives.
// Consume from an app as: import { Example } from "@repo/ui/example";
export function Example({ children }: { children: React.ReactNode }) {
	return <div>{children}</div>;
}
